<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xu.eureka.client.mapper.LogsMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.xu.eureka.client.entity.Logs">
        <id column="lid" property="lid" />
        <result column="ip" property="ip" />
        <result column="uid" property="uid" />
        <result column="type" property="type" />
        <result column="detail" property="detail" />
        <result column="operation_data" property="operationData" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        lid, ip, uid, type, detail, operation_data
    </sql>

</mapper>
